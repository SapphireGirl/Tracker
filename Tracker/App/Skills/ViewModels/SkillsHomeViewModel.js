productModule.controller("skillsHomeViewModel", function ($scope, skillsService, $http, $q, $routeParams, $window, $location, viewModelHelper) {

    $scope.viewModelHelper = viewModelHelper;
    $scope.skillsService = skillsService;


    var initialize = function () {

    }

    initialize();
});
